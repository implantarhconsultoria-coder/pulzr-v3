{
  "name": "pulzr-mobile-preview",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "python3 -m http.server 5173 --directory public --bind 0.0.0.0",
    "build": "node scripts/build-static.mjs",
    "preview": "python3 -m http.server 4173 --directory dist --bind 0.0.0.0"
  },
  "dependencies": {},
  "devDependencies": {},
  "engines": {
    "node": ">=20"
  }
}
